name: flutter_applicatione
description: "A new Flutter project."

publish_to: "none" # Si tu ne souhaites pas publier sur pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.1 # Mise à jour vers la version compatible
  firebase_auth: ^5.5.1
  cloud_firestore: 5.5.0
  cupertino_icons: ^1.0.8
  image_picker: ^0.8.5+3
  firebase_storage: ^12.4.4
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
